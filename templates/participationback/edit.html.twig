{% extends 'dashboard.html.twig' %}

{% block title %}Edit Participation{% endblock %}

{% block body %}
    <div class="container-fluid py-5" style="background-color: #fff; border-radius: 10px;">
       

        <div class="card shadow p-4">
            {{ include('participationback/_form.html.twig', {'button_label': 'Update'}) }}

            <div class="d-flex justify-content-between mt-4">
                <a href="{{ path('app_participationback_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Back to List
                </a>
                {{ include('participationback/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
