<form method="post" action="{{ path('app_rehla_delete', {'id': rehla.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rehla.id) }}">
    <button class="btn">Delete</button>
</form>
