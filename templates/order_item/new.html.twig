{% extends 'base.html.twig' %}

{% block title %}New Order Item{% endblock %}

{% block body %}
    <h1>Create New Order Item</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('order_item_index') }}">Back to list</a>
{% endblock %}
