{% extends 'dashboard.html.twig' %}

{% block title %}Edit Reponse{% endblock %}

{% block body %}
    <div style="max-width: 600px; margin: 40px auto; text-align: center; padding: 20px; border-radius: 10px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); background: #f9f9f9;">
        <h1 style="color: #333;">Edit Reponse</h1>

        <div style="margin: 20px 0;">
            {{ include('reponse/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <a href="{{ path('app_Areponse_index') }}" style="display: inline-block; margin-top: 10px; padding: 10px 15px; color: white; background: #007bff; text-decoration: none; border-radius: 5px;">Back to List</a>

        <div style="margin-top: 20px;">
            {{ include('reponseAdmin/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
