{% extends 'base.html.twig' %}

{% block title %}🛍️Détails de la commande{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="order-details-card shadow-lg p-5">
            <!-- Titre -->
            <h1 class="order-title text-center mb-4">🛍️ Détails de la commande #{{ order.id }}</h1>

            <!-- Informations principales -->
            <ul class="order-info-list">
                <li><strong>📅 Date de création :</strong> {{ order.createdAt|date('d/m/Y H:i') }}</li>
                <li><strong>📌 Statut :</strong> {{ order.status }}</li>
                <li><strong>💳 Prix total :</strong> {{ order.totalPrice }} €</li>
            </ul>

            <!-- Articles de la commande -->
            <h2 class="items-title mt-4">📦 Articles :</h2>
            <ul class="order-items-list">
                {% for item in order.orderItems %}
                    <li class="order-item">
                        <span class="item-name">📦 {{ item.product.name }}</span>
                        <span class="item-quantity">🔢 {{ item.quantity }} x</span>
                        <span class="item-price">💰 {{ item.product.price }} €</span>
                    </li>
                {% endfor %}
            </ul>

            <!-- Bouton retour -->
            <div class="text-center mt-4">
                <a href="{{ path('order_index') }}" class="btn btn-secondary">⬅️ Retour à la liste</a>
            </div>
        </div>
    </div>

    <style>
        /* Conteneur général */
        .container {
            max-width: 800px;
            margin: 0 auto;
            font-family: 'Arial', sans-serif;
            color: #2d3748;
        }

        /* Carte des détails */
        .order-details-card {
            background: linear-gradient(135deg, #f9fafc, #edf2f7);
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        /* Titre principal */
        .order-title {
            font-size: 28px;
            font-weight: bold;
            color: #2c5282;
        }

        /* Liste des informations de la commande */
        .order-info-list {
            list-style: none;
            padding: 0;
        }

        .order-info-list li {
            font-size: 16px;
            margin-bottom: 8px;
        }

        /* Section des articles */
        .items-title {
            font-size: 20px;
            font-weight: bold;
            color: #2c5282;
            margin-bottom: 12px;
        }

        .order-items-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .order-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 16px;
            background-color: #f7fafc;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 8px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
        }

        .item-name {
            flex: 1;
            font-weight: bold;
            color: #2d3748;
        }

        .item-quantity,
        .item-price {
            margin-left: 10px;
            color: #4a5568;
        }

        /* Bouton retour */
        .btn-secondary {
            background-color: #edf2f7;
            color: #2d3748;
            padding: 10px 20px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .btn-secondary:hover {
            background-color: #cbd5e0;
            transform: scale(1.05);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .order-item {
                flex-direction: column;
                align-items: flex-start;
            }

            .item-name,
            .item-quantity,
            .item-price {
                margin-left: 0;
                margin-bottom: 4px;
            }
        }
    </style>
{% endblock %}
