<form method="post" action="{{ path('app_compagnie_aerienne_delete', {'id': compagnie_aerienne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compagnie_aerienne.id) }}">
    <button class="btn">Delete</button>
</form>
