{% extends 'baseBlog.html.twig' %}

{% block title %}New Comment{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="col-md-8">
        <div class="card shadow-lg border-0 rounded-4">
            <div class="card-header bg-gradient text-white text-center py-4" style="background: linear-gradient(135deg, #6e8efb, #a777e3);">
                <h2 class="fw-bold">📝 Create new Comment</h2>
            </div>
            <div class="card-body p-4">
                {{ include('comment/_form.html.twig', {'button_label': '➕ Create Comment'}) }}
            </div>
        </div>


    
        </div>
    </div>
</div>
{% endblock %}
